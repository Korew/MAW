// https://www.npmjs.com/package/gulp-include

// $(function() {})

$('.section-carusel').slick({
  fade:true,
  dots: true,
  arrows:false,
  infinite: true,
  // autoplay: true,
  // autoplaySpeed:3000,
});